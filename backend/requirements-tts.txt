# OPTIONAL - text-to-speech / voice cloning with CosyVoice3.
#
# Install CUDA torch first, then install the CosyVoice3 inference dependencies:
#
#   python -m pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
#   python -m pip install -r requirements-tts.txt
#
# The CosyVoice source is vendored at vendor/CosyVoice and imported directly.
# Voice clip conversion (mp3/m4a/ogg -> wav) also needs ffmpeg on PATH:
#
#   winget install ffmpeg

-r requirements-cosyvoice3.txt
openai-whisper==20231117
pydub>=0.25.1
wheel>=0.47.0
