# Core backend - chat (Ollama) + speech-to-text (Whisper).
# Installs cleanly on Python 3.11-3.13, CPU or GPU, no PyTorch needed.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# File uploads (audio for STT)
python-multipart>=0.0.9

# Speech-to-text (runs on CPU via int8; no torch required; uses CTranslate2)
faster-whisper>=1.0.0

# Text-to-speech (voice output) is optional and heavy.
# It needs PyTorch + CosyVoice3. Install it separately when you want voice output:
#     pip install -r requirements-tts.txt
# See requirements-tts.txt for the torch install step.
